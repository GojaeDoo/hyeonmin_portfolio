{
  "compilerOptions": {
    "target": "es5", // 또는 es6, esnext
    "module": "esnext", // Next.js에서는 esnext를 사용
    "moduleResolution": "node", // node 스타일 모듈 해석
    "jsx": "preserve", // React 17 이상에서 JSX를 처리
    "strict": true, // 엄격한 타입 검사
    "esModuleInterop": true, // CommonJS 모듈을 ES 모듈처럼 사용할 수 있게 설정
    "skipLibCheck": true, // 라이브러리 타입 체크 생략
    "forceConsistentCasingInFileNames": true, // 대소문자 일관성 강제
    "noEmit": true, // JavaScript 파일을 출력하지 않음
    "incremental": true, // 점진적 컴파일
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true, // JavaScript 파일 허용
    "resolveJsonModule": true, // JSON 파일 가져오기 허용
    "isolatedModules": true // 각 파일을 독립적으로 처리
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts", // TypeScript 파일 포함
    "**/*.tsx" // JSX를 포함하는 TypeScript 파일 포함
  ],
  "exclude": [
    "node_modules"
  ]
}
